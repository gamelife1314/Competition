# 《未来战争》需求分析文档

> 本文档是我（AI 实现方）对赛题需求的全部理解，用于人工评审。**第六章「假设与待确认问题」最关键**——凡是我基于样例与 Demo 推断、而任务书未明说的点都列在那里，如有偏差请指出。
>
> 实现代码：`CoreGeek/`（Rust 1.72，`bash CoreGeek/run.sh <port>` 启动）

## 一、赛题概述与交付物

- 比赛形态：两支队伍同台对抗，各操控「人类方」在 41×32 地图上建防御工事，抵御每夜机器人浪潮并互相对抗；每场两轮（上/下半场互换位置），胜局积 3 分、平 1 分、负 0 分。
- **交付物是一个 HTTP 服务**：判题器以 `bash run.sh <port>` 拉起选手程序，程序监听 `0.0.0.0:<port>`；每回合判题器 POST 一个局面 JSON，选手程序须在 **5 秒内**返回指令 JSON。连接建立超 10 秒、响应超 5 秒、格式错误、指令字段缺失/无法识别，均计「异常响应」；**单队累计 5 次异常即被停止调度直至比赛结束**（等同弃权）。
- 指令「执行失败」（移动碰撞、攻击落点无目标等）**不计异常**——这决定了容错设计的分界线：结构级错误绝不能犯，执行级失败可以容忍。

## 二、对局节奏与时间轴

| 概念 | 数值 | 我的实现 |
|---|---|---|
| 总回合 | 1300 回合 = 10 天 | `day = (roundNo-1)/130 + 1` |
| 白天 | 每天前 70 回合 | `(roundNo-1)%130 < 70`；白天可建造/采集/买卖/做任务，**不可攻击** |
| 黑夜 | 每天后 60 回合 | 黑夜机器人出现，**仅黑夜可攻击**；白天第一回合清场 |
| 机器人 | 夜晚第 1 回合统一刷新，数量随天数增长 | 全图可见，含 `targetTeam`（攻击哪一方） |

每回合**每个角色至多 1 条指令**；结算顺序：武器攻击 > 机器人移动，伤害回合末统一结算。

## 三、世界规则理解

### 3.1 地图与区域
- 坐标原点在**左下角**，x∈[0,40]，y∈[0,31]；基地在左上与右下区域，2×2 大小，`pos` 传的是**左上角坐标**（结合 y 轴向上，我按 Demo 的足迹展开 `(x,y),(x+1,y),(x,y-1),(x+1,y-1)` 处理——见待确认 Q1）。
- 可建造分区：**蓝区只能建武器、黄区只能建墙**，具体格位任务书只给了截图。我采用 Demo 的几何假设：武器 = 基地足迹外扩 1 格的环，墙 = 外扩 2 格的环（留一个出入口），并用**失败学习**兜底：同一格位建造失败 2 次即拉黑换格（见 Q2）。
- 矿区（石/铁/铜）随机刷新、采 10 次消失后异地重刷；矿、任务点、中立单位、双方建筑/角色/机器人均阻挡移动；8 方向移动，对角不构成阻挡。
- 距离一律为**切比雪夫距离** `max(|dx|,|dy|)`。

### 3.2 视野
- 己方所有单位视野 4 格、全局共享；机器人全图可见；**敌方基地与围墙全局可见**，其余敌方单位需进入视野才会出现在 `teamEnemy.roles` 中。→ 实现中不自行做视野推算，直接以请求给出的数据为准；敌方「消失」的单位按最后可见位置不再追踪（见 Q7）。

### 3.3 我方单位
| 单位 | 关键属性 | 我的运用 |
|---|---|---|
| 基地 | HP 1500/3000/4500（lv1/2/3） | 升级券优先度次于武器；生存积分核心 |
| 加特林 | 射程 3/5/7，攻击力 10×弹数，弹数=等级，多目标须同 90° 锥 | 锥内贪心集火，弹道首个机器人承伤 |
| 电磁狙击炮 | 射程 6/8/10，能量 10/20/30 沿弹道穿透 | 枚举落点选穿透总伤害最大的直线 |
| 火箭发射台 | 射程 10/15/全图，20 中心+10 溅射×等级枚数，冷却 3 回合 | 3×3 集群溅射评分，多枚可叠落点；冷却>0 跳过 |
| 围墙 | HP 1000/1500/2000，石头×1 可重复建，拆不回收 | 环绕基地留口，石头批量化采集后铺设 |
| 开拓者 | HP200、背包 40，唯一可接任务/召唤宝藏 | 白天任务与宝藏动线，夜晚操控炮塔 |
| 工人 | HP220、背包 100，唯一可采集/建造/拆除 | 经济主力，夜晚操控炮塔 |

- 武器全局至多 3 座；在已有武器格重建会**覆盖**（新 lv1）——实现上绝不向已有武器的格位发 build。
- 角色阵亡：次日白天开始后 20 回合基地复活，背包保留。

### 3.4 指令全集（12 种）与合法性约束
`move / attack / sell / buy / build / remove / acceptTask / submitAnswer / summonTreasure / use / drop / collect`
实现内置**校验层**（`validate.rs`），出网前逐条核查：动作-角色匹配（build/collect/remove→工人；acceptTask/submitAnswer/summonTreasure→开拓者）、昼夜限制、相邻 1 格要求（build/collect/remove/WallFixer/升级券/summonTreasure）、必填字段（move/attack 的 targetPos、buy/sell 的 name、attack 的 controllerId）、攻击目标数=武器等级、加特林锥角预检、物品必须在背包、金币/库存充足。**任何一项不过直接丢弃该指令**（宁可空过，不送异常）。

### 3.5 经济与商店
- 初始 75 金币；矿石卖小贩换金币，价格随官方新闻波动（请求中 `vendorShopList` 是**实时价格**，直接以此决策，基线=历史最低价）。
- 武器建造 25 金币/座；墙 = 石头×1。
- 商店：升级券（武器 100/150、基地 100/150、墙 20/30，需走到建筑 1 格内 `use`+`targetPos`，满级再用不消耗）、消耗品（修复包/药剂 10、眩晕/炸弹 100、召唤令 20~200，召唤令每日限 10 张，眩晕与炸弹仅对机器人有效且无距离限制）、任务用品（6 种×15 金币，仅用于宝藏献祭，**召唤动作合法即消耗**）。
- 当回合矿石不足以平分时每个工人各得 1 个（不影响决策，仅影响采集效率预期）。

### 3.6 三类任务
| 类型 | 通道 | 我的实现 |
|---|---|---|
| 推理类 | 官方新闻 → 矿价/停产变化 | `news.rs` 关键词解析出「矿种×停采日窗」，工人避开停产矿、涨价期优先抛售对应存货 |
| 长上下文类 | 民间传闻逐日累积 → 推断宝藏（地点/条件/时间） | `treasure.rs`：逐日存传闻，用每日 3 次 LLM 额度请裁判侧 LLM 输出严格 JSON（坐标/献祭品/开启日），购物品→踩点→到期献祭；结果码 2=推迟一天重试，3=带反馈重推理，1/4=终止 |
| 自进化类 | 任务点主动领取，沙盒 shell/python 交互 | `task.rs` 状态机：acceptTask → 读 phaseTask → prompt（任务期不限次）→ 提取代码块经 executeCmd 沙盒执行 → 解析 `[exitCode:N]` 输出提取 `ANSWER:` → submitAnswer；答错（errorCode 2）带反馈重试；超时前 2 回合强制提交最优答案；成功脚本沉淀 SOP 缓存，同类任务直接复用（压缩完成回合数拿满 `5×标准/实际` 加速分） |

- 任务点机制：双方各 2 个（任务点 2 占两格）；仅本队开拓者有效；任务结束后冷却 30 回合；**领取后离开任务点 1 格范围 / 超时 / 死亡均判任务结束** → 任务期间开拓者绝不移动。
- 积分：`score1` 任务分（完成=奖励+5×标准回合/实际回合；部分完成=奖励×通过率，金币=奖励×通过率）；`score2` 击杀分（小1 中2 大4 BOSS10）；`score3` 生存分（Σ 10×day×存活系数，基地被毁当天起归零）。
- **LLM 额度**：每游戏日 3 次（每天第一回合重置），任务执行期间不限次且不计额度。每回合响应中 `prompt` 只有一个 → 实现里任务优先占用，宝藏仅在无任务且额度富余时发起。

### 3.7 胜负与异常
- 结束：双方各 5 次异常 / 1300 回合打完 / 双方基地均毁。
- 判定：同回合双毁且同分=平；先后被毁=先毁者负；均未毁=比积分。
- 异常三类：响应超时（连接 10s/响应 5s）、格式错误、指令错误（字段缺失/动作码非法）。→ 对应实现：启动即同步 bind、决策全程无阻塞 IO（实测单回合 <10ms）、永不动态拼接非法 JSON（serde 序列化）、校验层兜底；进程 panic 只损失单连接（未用 panic=abort），任何内部错误返回 `{"roleCommandMap":{},"prompt":"","executeCmd":""}`。

## 四、需求 → 实现映射

| 需求点 | 代码位置 |
|---|---|
| HTTP 服务、端口参数、5s 时限 | `main.rs`（同步 bind）、`server.rs`（hyper HTTP/1.1） |
| 请求宽容解析（缺字段不炸） | `protocol.rs`（serde `#[serde(default)]`） |
| 昼夜历法/地图视图/阻挡 | `model.rs` |
| A* 寻路（走向「可动作格」） | `path.rs` |
| 跨回合记忆（新闻/任务/宝藏/预算/失败反馈） | `state.rs` |
| 白天经济与建造 | `brain/day.rs`、`brain/economy.rs` |
| 夜晚布防与三武器弹道模型 | `brain/night.rs`、`brain/combat.rs` |
| 官方新闻解析（停产/涨价窗口） | `brain/news.rs` |
| 自进化任务闭环 + SOP 自进化 | `brain/task.rs` |
| 宝藏推理与献祭闭环 | `brain/treasure.rs` |
| 异常防护（5 次出局） | `brain/validate.rs` + 全局兜底 |
| 赛后复盘日志 | `log.rs`（见第七章） |

## 五、核心策略取舍

1. **生存分保底**：墙环+三塔尽早成型，基地升级券次优先；夜晚Bomb/眩晕留给威胁集群。
2. **击杀分最大化**：按武器弹道模型精确计算期望伤害与击杀奖励（含威胁加权：`targetTeam`=我方的机器人优先）。
3. **主动胜负手**：射程内无机器人时 opportunistically 打敌方基地/单位（火箭 lv3 全图射程是远程压制手段）；金币富余（≥500）时买 BOSS 召唤令骚扰对面（每日 10 张上限内）。
4. **任务分**：开拓者任务冷却期才去做宝藏线；任务期间原地不动保任务不被中断。
5. **经济效率**：石头批量采 6 个再铺墙（减少空走）；实时比价卖矿；停产矿种不采、存货趁涨价抛售。
6. **黄昏预就位**：白天最后 12 回合各角色提前走到夜晚配对的炮塔旁，夜临即开火。

## 六、假设与待确认问题（重点评审）

| # | 问题 | 我的假设/处理 | 风险 |
|---|---|---|---|
| Q1 | 基地 `pos` 为「左上角」，但地图 y 轴向上 | 按 Demo 足迹 `(x,y),(x+1,y),(x,y-1),(x+1,y-1)` 展开（即向右、向下扩展） | 若方向反了，墙环/塔位整体偏移；Demo 经过实战可参考 |
| Q2 | 蓝区/黄区具体格位未知（任务书仅截图） | 武器=足迹外扩1格环、墙=外扩2格环（同 Demo），建造失败2次自动拉黑换格位 | 前几次建造可能浪费回合，靠失败学习收敛 |
| Q3 | attack 指令的 key 是谁 | 按 `response.txt`：key=**炮塔 ID**，`controllerId`=操控角色 ID | 若实际相反，夜晚攻击全部失效（校验层不会报异常但无输出）——样例明确，风险低 |
| Q4 | 加特林/火箭目标数必须等于等级？ | 是（接口文档「目标位置数与当前武器等级数相同」）；目标不足时重复填充最优落点（火箭叠加伤害合法，加特林重复弹道可能浪费但不非法） | 若判「执行失败」仅损失一次攻击 |
| Q5 | 攻击路径被己方墙/建筑阻挡？ | 假设**只被机器人拦截**（任务书只提「命中弹道上最近的一台机器人」）；墙不挡弹道 | 若墙挡弹道，贴墙射击可能自伤逻辑失效，需观察日志调整站位 |
| Q6 | 机器人刷新点与寻路目标 | 未知；按「冲基地/墙」设防（威胁加权+墙环）；不做刷新点预测 | 若机器人主攻任务点/商店方向，防守重心可能偏差 |
| Q7 | 敌方非建筑单位的记忆 | 不追踪消失的敌方单位（只打当前可见） | 夜战对敌方角色的压制较弱 |
| Q8 | `phaseTask` 在任务进行期间是否每回合都携带原文 | 是（「当前已领取任务的原文描述”）；以「描述已存过且 phaseTask 变空」判定任务结束 | 若判题器中途清空该字段会误判任务结束（有 3 回合宽限的接受态兜底） |
| Q9 | `errors` 数组的时效性 | 假设只含**本回合新产生**的错误；答错(码2)仅在「刚提交过答案」状态下消费，防止陈旧错误反复触发重试 | 样例 request 常驻 errorCode 2，若判题器行为如样例（错误常驻多回合），我的去重逻辑也已覆盖 |
| Q10 | LLM 的 `prompt` 每回合只能发一条？ | 是；任务优先、宝藏其次；宝藏推理失败最多重问 3 次 | 任务与宝藏冲突时宝藏延迟 |
| Q11 | `executeCmd` 的执行时机 | 「判题器会在本回合执行」→ 结果下一回合经 `lastCmdResult` 返回；沙盒无外网、15s 上限 | 任务闭环每步间隔 1 回合，状态机按此设计 |
| Q12 | 宝藏祭坛是否为地图上的可见格 | 假设是普通格（传闻可推理出坐标）；开拓者走到其相邻格、`targetPos`=祭坛格献祭 | 若祭坛需要特殊 zone 标识，需按日志中结果码 2/3 的反馈迭代 |
| Q13 | 召唤令「对方下个夜晚+1」的目标 | `use` 无需参数，默认作用于对面 | 样例明确，风险低 |
| Q14 | 上下半场换边后队伍 `type` 是否互换 | 以每回合请求中的 `teamOur.type` 为准动态判断任务点归属，不写死 | 已按请求驱动，天然兼容 |

## 七、日志与赛后优化方法

- 运行目录生成 `coregeek.log`（可用环境变量 `COREGEEK_LOG` 改路径），JSONL 格式，每行一个事件。
- **`round` 事件（每回合一条）**：回合/天数/昼夜、金币、总分、基地血量等级、存活机器人数、三塔状态（等级/血量/冷却）、三角色（血量/背包占用）、**我方全部指令**、**上回合执行失败的指令及原因上下文**、判题器错误码、任务状态机阶段、宝藏阶段、prompt/executeCmd 长度、当日 LLM 已用次数、决策耗时 ms。
- **专项事件**：`startup`、`news_official`（官方消息原文摘要）、`mine_outage`（解析出的停产窗口）、`build_blacklisted`（建造失败拉黑）、`task_accept/task_started/task_ended`（含答错次数与最优答案）、`llm_resp/cmd_result/prompt_sent/cmd_sent`（LLM 与沙盒流量摘要）、`treasure_plan/treasure_result`（推理结果与献祭结果码）。
- **优化闭环**：拿到对战日志后重点看——① `failures` 高频动作（暴露规则理解偏差，如 Q2/Q5）；② `score` 增长曲线与 `stationHp`（防守强度）；③ `task` 阶段流转耗时（LLM/沙盒往返效率）；④ `treasure_result` 结果码（推理准确性）；⑤ 每晚 `robots` 与塔血量（火力配比是否需要调整升级优先级）。

## 八、构建、运行与测试

```bash
cd CoreGeek
cargo build --release --locked   # rust-toolchain.toml 自动选用 1.72
cargo test                       # 14 个测试：弹道/解析/历法 + 回放 docs/request.txt
bash run.sh 8080                 # 判题器启动方式
```

依赖锁定说明：cargo 1.72 无 MSRV 感知解析，`Cargo.toml` 内全部 `=` 精确锁版（serde 1.0.193 / serde_json 1.0.108 / tokio 1.35.1 / hyper 1.1.0 / hyper-util 0.1.3 / http-body-util 0.1.0），`Cargo.lock` 随仓库提交，**勿升级**（新版传递依赖 hashbrown 0.17 需 edition 2024，1.72 无法编译）。
